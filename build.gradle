// Root project build.gradle

plugins {
    id 'com.android.application' version '7.3.1' apply false
}

// Ensure repositories are defined
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
